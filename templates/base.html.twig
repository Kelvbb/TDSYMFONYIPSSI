<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Paradox{% endblock %}</title>
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 100 100%22><text y=%22.9em%22 font-size=%2290%22>◻</text></svg>">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        :root { --minimal-black: #111; --minimal-gray: #666; --minimal-border: #e0e0e0; }
        body { background: #fff; color: var(--minimal-black); font-family: system-ui, -apple-system, sans-serif; }
        .navbar-minimal { background: #fff !important; border-bottom: 1px solid var(--minimal-border); }
        .navbar-minimal .navbar-brand, .navbar-minimal .nav-link { color: var(--minimal-black) !important; }
        .navbar-minimal .nav-link:hover { color: var(--minimal-gray) !important; }
        .navbar-minimal .dropdown-menu { border: 1px solid var(--minimal-border); box-shadow: none; }
        .navbar-minimal .dropdown-item:hover { background: #f5f5f5; }
        .navbar-toggler { border-color: var(--minimal-border); }
        .navbar-toggler-icon { filter: invert(0); }
        .btn-minimal { border: 1px solid var(--minimal-black); color: var(--minimal-black); background: transparent; }
        .btn-minimal:hover { background: var(--minimal-black); color: #fff; border-color: var(--minimal-black); }
        .btn-minimal-solid { background: var(--minimal-black); color: #fff; border: 1px solid var(--minimal-black); }
        .btn-minimal-solid:hover { background: #333; color: #fff; border-color: #333; }
        .card-minimal { border: 1px solid var(--minimal-border); box-shadow: none; background: #fff; }
        .badge-minimal { background: transparent; color: var(--minimal-black); border: 1px solid var(--minimal-black); font-weight: normal; }
        .alert-minimal { border: 1px solid var(--minimal-border); background: #fafafa; color: var(--minimal-black); }
        .alert-minimal.alert-success { border-color: var(--minimal-gray); }
        .alert-minimal.alert-danger { border-color: var(--minimal-black); background: #fff; }
        a { color: var(--minimal-black); text-decoration: none; }
        a:hover { color: var(--minimal-gray); }
        .table { color: var(--minimal-black); }
        .table td, .table th { border-color: var(--minimal-border); vertical-align: middle; }
        .form-control, .form-select { border-color: var(--minimal-border); }
        .form-control:focus, .form-select:focus { border-color: var(--minimal-black); box-shadow: 0 0 0 1px var(--minimal-black); }
        .breadcrumb-item + .breadcrumb-item::before { color: var(--minimal-gray); }
    </style>
    {% block stylesheets %}{% endblock %}
</head>
<body class="d-flex flex-column min-vh-100">
    <nav class="navbar navbar-expand-lg navbar-minimal">
        <div class="container">
            <a class="navbar-brand fw-normal" href="{{ path('home') }}">Paradox</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Menu">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav flex-grow-1 justify-content-center">
                    <li class="nav-item"><a class="nav-link" href="{{ path('home') }}">Accueil</a></li>
                    <li class="nav-item"><a class="nav-link" href="{{ path('blog_index') }}">Articles</a></li>
                    {% if is_granted('ROLE_ADMIN') %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('admin_dashboard') }}">Admin</a>
                    </li>
                    {% endif %}
                </ul>
                <ul class="navbar-nav">
                    {% if app.user %}
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle d-flex align-items-center gap-1" href="#" id="userDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" fill="currentColor" viewBox="0 0 16 16" aria-hidden="true"><path d="M8 8a3 3 0 1 0 0-6 3 3 0 0 0 0 6zm2-3a2 2 0 1 1-4 0 2 2 0 0 1 4 0zm4 8c0 1-1 1-1 1H3s-1 0-1-1 1-4 6-4 6 3 6 4zm-1-.004c-.001-.246-.154-.986-.832-1.664C11.516 10.68 10.289 10 8 10c-2.29 0-3.516.68-4.168 1.332-.678.678-.83 1.418-.832 1.664h10z"/></svg>
                            {{ app.user.firstName ?? app.user.userIdentifier }}
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end">
                            <li><a class="dropdown-item" href="{{ path('profile_show') }}">Profil</a></li>
                            <li><a class="dropdown-item" href="{{ path('profile_edit') }}">Modifier le profil</a></li>
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item" href="{{ path('app_logout') }}">Déconnexion</a></li>
                        </ul>
                    </li>
                    {% else %}
                    <li class="nav-item"><a class="nav-link" href="{{ path('app_login') }}">Connexion</a></li>
                    <li class="nav-item"><a class="nav-link" href="{{ path('app_register') }}">Inscription</a></li>
                    {% endif %}
                </ul>
            </div>
        </div>
    </nav>

    <main class="container flex-grow-1 d-flex flex-column py-5">
        {% for label, messages in app.flashes %}
            {% for message in messages %}
                <div class="alert alert-minimal alert-{{ label == 'error' ? 'danger' : label }} alert-dismissible fade show" role="alert">
                    {{ message }}
                    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Fermer"></button>
                </div>
            {% endfor %}
        {% endfor %}
        {% block body %}{% endblock %}
    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    {% block javascripts %}{% endblock %}
</body>
</html>
